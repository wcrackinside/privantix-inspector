# Rules (governance)

- **Detection & profiling rules:** [`detectors/`](../detectors/), [`analyzer/rules.go`](../analyzer/rules.go)
- **ACL / trust helpers:** [`utils/trusted_groups.go`](../utils/trusted_groups.go)

Governance-related CLI flags (e.g. `--hide-samples`, `--trusted-groups`) are documented in [`docs/usage.md`](../docs/usage.md).
