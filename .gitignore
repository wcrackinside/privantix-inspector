# Archives (avoid committing site snapshots)
*.zip

# Build / binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
/dist/

# Go
coverage.out
*.test

# Local outputs and generated reports (keep examples/ if needed)
/output/

# Optional GitHub API cache (generate with scripts/sync_releases_cache.py)
privantix_site/data/releases-cache.json

# Env / secrets
.env
.env.*
!.env.example

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~
